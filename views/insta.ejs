<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css"
      integrity="sha512-2SwdPD6INVrV/lHTZbO2nodKhrnDdJK9/kg2XD1r9uGqPo1cUbujc+IYdlYdEErWNu69gVcYgdxlmVmzTWnetw=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <title>instagram page</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: Arial, Helvetica, sans-serif;
      }

      body {
        min-height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background: #fafafa;
      }

      /* Main Card */
      .div {
        background: #fff;
        border-radius: 18px;
        padding: 1.5rem 2rem;
        width: 400px;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
      }

      /* Headings */
      .div h1 {
        text-align: center;
        letter-spacing: 2px;
        margin-bottom: 0.4rem;
      }

      .div h2 {
        text-align: center;
        font-size: 1.1rem;
        font-weight: 500;
        margin-bottom: 1.2rem;
      }

      .dp {
        justify-content: center;
        align-items: center;
        display: flex;
        margin-bottom: 10px;
      }
      .dpImage {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid #ffffff;
        box-shadow: 0px 0px 8px #0000006f;
      }

      /* Stats Section */
      .section {
        display: flex;
        justify-content: space-between;
        margin-bottom: 1.3rem;
        text-align: center;
      }

      .section div {
        flex: 1;
        font-weight: bold;
      }

      .section span {
        font-size: 0.8rem;
        color: #777;
      }

      /* Buttons */
      button {
        width: 48%;
        padding: 0.5rem;
        border-radius: 8px;
        border: none;
        font-weight: 600;
        cursor: pointer;
      }

      button:first-of-type {
        background: #0095f6;
        color: #fff;
      }

      button:last-of-type {
        background: #efefef;
      }

      button:hover {
        opacity: 0.9;
      }

      /* Posts Title */
      h3 {
        margin: 1.3rem 0 0.8rem;
      }

      /* Posts Grid */
      .postSection {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 0.8rem;
      }

      /* Single Post */
      .post {
        background: #fafafa;
        border-radius: 12px;
        padding: 0.4rem;
        text-align: center;
      }

      .postNo {
        font-size: 0.75rem;
        margin-bottom: 0.3rem;
        color: #555;
      }

      /* Post Image */
      .post img {
        width: 100%;
        height: 110px;
        object-fit: cover;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      }

      /* Likes & Comments */
      .extraFeature {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 0.35rem;
        padding: 0 0.3rem;
        font-size: 0.8rem;
      }

      .extraFeature i {
        display: flex;
        align-items: center;
        gap: 4px;
        cursor: pointer;
      }

      .extraFeature .likeCount {
        color: black;
        font-weight: 200;
      }

      .extraFeature .fa-comment {
        color: #555;
        margin-right: 5px;
      }

      /* Hover effect */
      .post:hover img {
        transform: scale(1.03);
        transition: 0.25s ease;
      }
    </style>
  </head>
  <body>
    <div class="div">
      <h1>INSTAGRAM</h1>
      <h2>Welcome! @ <%= data.name %><br /></h2>
      <div class="dp">
        <img src="<%= data.dp %>" alt="dogDp" class="dpImage" />
      </div>
      <div class="section">
        <div class="post">
          <%= data.posts.length %> <br />
          <span>Posts</span>
        </div>

        <div class="followers">
          <%= data.followers %> <br />
          <span>Followers</span>
        </div>

        <div class="followers">
          <%= data.following %> <br />
          <span>Following</span>
        </div>
      </div>

      <button>Follow</button>
      <button>Message</button>

      <h3>Posts</h3>
      <div class="postSection">
        <% data.posts.forEach((post, index)=>{ %>

        <div class="post">
          <div class="postNo">Post <%= index +1 %></div>
          <img src="<%=post.image%>" alt="img" />

          <div class="extraFeature">
            <i class="fa-solid fa-heart" style="color: #fa0000">
              <span class="likeCount"><%= post.likes %></span></i
            >
            <i class="fa-regular fa-comment"><%= post.comments %></i>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
  </body>
</html>
